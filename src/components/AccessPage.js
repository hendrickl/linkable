import React from "react";
import { ethers } from "ethers";

const AccessPAge = () => {
  // Interface d'accueil simple invitant à se connecter
  // sur MetaMask

  // Check isWeb3
  // Si MetaMask
  return <MainPage />;
};
