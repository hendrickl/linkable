import React from "react";
import { ethers } from "ethers";

const AccessPAge = () => {
  // Interface d'accueil simple invitant à se connecter sur MetaMask
  // Une fois la connection faite, ouverture de MainPage

  return <MainPage />;
};
